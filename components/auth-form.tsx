"use client";import { useActionState } from "react";import { login,register,FormState } from "@/app/actions";
const initial:FormState={};export function AuthForm({mode}:{mode:"login"|"register"}){const [state,action,pending]=useActionState(mode==="login"?login:register,initial);return <form action={action} className="card mx-auto mt-10 max-w-md space-y-4 p-6">{mode==="register"&&<label><span className="label">Display name</span><input name="displayName" required maxLength={60}/></label>}<label><span className="label">Email address</span><input name="email" type="email" required autoComplete="email"/></label><label><span className="label">Password</span><input name="password" type="password" required minLength={8} autoComplete={mode==="login"?"current-password":"new-password"}/></label>{state.error&&<p role="alert" className="rounded-lg bg-red-50 p-3 text-sm text-red-700">{state.error}</p>}<button disabled={pending} className="btn-primary w-full">{pending?"Please wait…":mode==="login"?"Sign in":"Create account"}</button></form>}
