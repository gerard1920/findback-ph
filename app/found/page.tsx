import { db } from "@/lib/db";import { ItemCard } from "@/components/item-card";
export default async function Found({searchParams}:{searchParams:Promise<{q?:string;city?:string;category?:string}>}){const p=await searchParams;const q=p.q?.trim();const items=await db.item.findMany({where:{type:"FOUND",status:{in:["ACTIVE","MATCHED","CLAIM_PENDING"]},...(q?{OR:[{title:{contains:q,mode:"insensitive"}},{description:{contains:q,mode:"insensitive"}}]}:{}),...(p.city?{city:{contains:p.city,mode:"insensitive"}}:{})},include:{images:{take:1},category:true},orderBy:{createdAt:"desc"},take:24});return <main className="container-page py-10"><h1 className="text-3xl font-bold">Found Items</h1><p className="mt-2 text-slate-600">Browse recently reported found belongings.</p><form className="card mt-7 flex gap-3 p-4"><input name="q" defaultValue={q} placeholder="Search items"/><input name="city" defaultValue={p.city} placeholder="City"/><button className="btn-primary">Search</button></form>{items.length?<div className="mt-8 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map(i=><ItemCard item={i} key={i.id}/>)}</div>:<div className="card mt-8 p-10 text-center">No found items match your search.</div>}</main>}
