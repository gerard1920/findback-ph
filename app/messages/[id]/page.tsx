"use client";
import { use, useEffect, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
type Message={id:number;body:string;sender_id:number;full_name:string;created_at:string};
export default function Conversation({params}:{params:Promise<{id:string}>}){const {id}=use(params);const router=useRouter();const [messages,setMessages]=useState<Message[]>([]);const [body,setBody]=useState("");const [error,setError]=useState<string>();const [sending,setSending]=useState(false);const load=()=>fetch(`/api/messages.php?id=${encodeURIComponent(id)}`,{credentials:"same-origin"}).then(async r=>{if(r.status===401){router.replace("/login");return;}const d=await r.json();if(!r.ok)throw new Error(d.error);setMessages(d.data?.messages??[])}).catch(e=>setError(e instanceof Error?e.message:"Unable to load messages."));useEffect(()=>{load()},[id]);async function send(e:React.FormEvent){e.preventDefault();if(!body.trim())return;setSending(true);setError(undefined);try{const r=await fetch("/api/messages.php",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"same-origin",body:JSON.stringify({conversationId:Number(id),body})});const d=await r.json();if(!r.ok)throw new Error(d.error);setBody("");await load()}catch(e){setError(e instanceof Error?e.message:"Unable to send message.")}finally{setSending(false)}}return <main className="container-page max-w-3xl py-10"><Link className="text-sm font-semibold text-blue-700" href="/messages">← All messages</Link><h1 className="mt-3 text-3xl font-bold">Conversation</h1><div className="card mt-6 space-y-3 p-5">{messages.length?messages.map(m=><div className="rounded-lg bg-slate-50 p-3" key={m.id}><b className="text-sm">{m.full_name}</b><p className="mt-1 whitespace-pre-wrap">{m.body}</p><time className="mt-2 block text-xs text-slate-500">{new Date(m.created_at).toLocaleString("en-PH")}</time></div>):<p className="text-slate-600">No messages yet.</p>}</div><form onSubmit={send} className="mt-4 flex gap-3"><input value={body} onChange={e=>setBody(e.target.value)} maxLength={2000} placeholder="Write a message" aria-label="Message"/><button disabled={sending} className="btn-primary">{sending?"Sending…":"Send"}</button></form>{error&&<p role="alert" className="mt-3 text-sm text-red-700">{error}</p>}</main>}
