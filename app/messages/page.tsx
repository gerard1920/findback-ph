"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { useRouter } from "next/navigation";
type Conversation={id:number;item_id:number;title:string;created_at:string;last_message_at:string|null;unread_count:number};
export default function Messages(){const router=useRouter();const [items,setItems]=useState<Conversation[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{fetch("/api/messages.php",{credentials:"same-origin"}).then(async r=>{if(r.status===401){router.replace("/login");return;}const data=await r.json();if(!r.ok)throw new Error(data.error);setItems(data.data?.conversations??[])}).catch(()=>setItems([])).finally(()=>setLoading(false))},[router]);return <main className="container-page max-w-4xl py-10"><h1 className="text-3xl font-bold">Messages</h1><p className="mt-2 text-slate-600">Communicate privately without sharing your phone number or email.</p>{loading?<p className="mt-8 text-slate-600">Loading conversations…</p>:items.length?<div className="mt-7 space-y-3">{items.map(c=><Link className="card flex items-center justify-between p-4 hover:border-blue-300" href={`/messages/${c.id}`} key={c.id}><div><b>{c.title}</b><p className="mt-1 text-sm text-slate-500">{c.last_message_at?`Last message ${new Date(c.last_message_at).toLocaleString("en-PH")}`:"No messages yet"}</p></div>{c.unread_count>0&&<span className="rounded-full bg-blue-700 px-2 py-1 text-xs font-bold text-white">{c.unread_count}</span>}</Link>)}</div>:<div className="card mt-7 p-10 text-center text-slate-600">No conversations yet. Open an item and contact its reporter to begin.</div>}</main>}
