import { NextResponse } from "next/server";import { requireUser } from "@/lib/auth";import { db } from "@/lib/db";export async function POST(_:Request,{params}:{params:Promise<{id:string}>}){const user=await requireUser();const id=(await params).id;await db.item.updateMany({where:{id,ownerId:user.id},data:{status:"RESOLVED"}});return NextResponse.redirect(new URL(`/items/${id}`,process.env.NEXT_PUBLIC_APP_URL||"http://localhost:3000"),303)}
