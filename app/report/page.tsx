import { requireUser } from "@/lib/auth";import { db } from "@/lib/db";import { redirect } from "next/navigation";export default async function Report({searchParams}:{searchParams:Promise<{item?:string}>}){await requireUser();const {item}=await searchParams;if(!item)return <main className="container-page py-10"><h1 className="text-3xl font-bold">Report a concern</h1><p className="mt-3 text-slate-600">Open a listing and choose “Report listing” to report an item.</p></main>;async function submit(fd:FormData){"use server";const u=await requireUser();await db.report.create({data:{itemId:item!,reporterId:u.id,reason:fd.get("reason") as never,details:String(fd.get("details")||"")}});redirect("/lost")}return <main className="container-page max-w-xl py-10"><h1 className="text-3xl font-bold">Report listing</h1><form action={submit} className="card mt-6 space-y-4 p-5"><label><span className="label">Reason</span><select name="reason"><option value="FAKE_LISTING">Fake listing</option><option value="SCAM">Scam</option><option value="HARASSMENT">Harassment</option><option value="STOLEN">Stolen item</option><option value="INAPPROPRIATE">Inappropriate content</option><option value="SPAM">Spam</option><option value="SUSPICIOUS">Suspicious behavior</option></select></label><label><span className="label">Details (optional)</span><textarea name="details" rows={4}/></label><button className="btn-primary">Submit report</button></form></main>}
