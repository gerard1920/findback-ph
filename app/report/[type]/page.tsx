import { notFound } from "next/navigation";import { db } from "@/lib/db";import { ReportForm } from "@/components/report-form";import { currentUser } from "@/lib/auth";import { redirect } from "next/navigation";
export default async function ReportPage({params}:{params:Promise<{type:string}>}){const {type}=await params;if(type!=="lost"&&type!=="found")notFound();if(!await currentUser())redirect("/login");const categories=await db.category.findMany({orderBy:{name:"asc"}});return <main className="container-page max-w-3xl py-10"><h1 className="text-3xl font-bold">Report {type} item</h1><p className="mt-2 text-slate-600">Share only approximate public information. You can use private details to verify the rightful owner.</p><ReportForm type={type.toUpperCase() as "LOST"|"FOUND"} categories={categories}/></main>}
